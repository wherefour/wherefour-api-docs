{
  "summary": "Formulas",
  "description": "Formulas",
  "get": {
    "tags": [
      "Formulas"
    ],
    "description": "Lists formulas with optional name, type and search_type filters",
    "summary": "List formulas",
    "operationId": "list_formulas",
    "parameters": [
      {
        "schema": { "type": "string" },
        "in": "query",
        "name": "name",
        "required": false,
        "description": "Name of the formula to search for"
      },
      {
        "schema": { "type": "string" },
        "in": "query",
        "name": "type",
        "required": true,
        "description": "Type of formula to search for, one of: (boms, kits, disassemblies)"
      },
      {
        "schema": { "type": "string" },
        "in": "query",
        "name": "search_type",
        "required": true,
        "description": "Type of data to search for, one of: (formula, formula_version)"
      }
    ],
    "responses": {
      "200": {
        "description": "OK",
        "content": {
          "application/json": {
            "schema": {
              "$ref": "../components/schemas/formulas.json"
            },
            "examples": {
              "Example": {
                "value": {
                  "$ref": "../components/examples/responses/formulas.json"
                }
              }
            }
          }
        }
      }
    }
  }
}
